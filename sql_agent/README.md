# SQL-Agent CLI

ИИ-агент для анализа данных из CSV файлов с помощью SQL. Этот инструмент позволяет задавать вопросы на естественном языке о ваших CSV данных, автоматически генерируя и выполняя безопасные SQL запросы для получения аналитики.

## Возможности

- 🤖 **Анализ с помощью ИИ**: Задавайте вопросы на естественном языке, получайте ответы на основе SQL
- 🔒 **Безопасность прежде всего**: Валидация SQL по белому списку, разрешены только SELECT запросы
- 📊 **CSV в SQL**: Автоматический импорт CSV в SQLite с умным определением типов колонок
- 🎨 **Красивый CLI**: Богатый интерфейс терминала с таблицами, прогресс-барами и цветами
- ⚡ **Быстрый и легкий**: Локальная база данных SQLite, никаких внешних зависимостей
- 🛠️ **Интеграция с LangChain**: Продвинутый tool calling с OpenAI GPT-4

## Быстрый старт

### Установка

1. Склонируйте или скачайте проект:
```bash
git clone <repository-url>
cd sql_agent
```

2. Установите зависимости:
```bash
pip install -e .
```

3. Настройте ваш OpenAI API ключ:
```bash
export OPENAI_API_KEY="ваш_api_ключ_здесь"
```

Или создайте файл `.env`:
```bash
cp .env.example .env
# Отредактируйте .env и добавьте ваш API ключ
```

### Базовое использование

1. **Загрузите ваши CSV данные:**
```bash
sql-agent load data/sales.csv --table sales
```

2. **Задавайте вопросы о ваших данных:**
```bash
sql-agent ask "Какие топ-5 товаров по выручке?"
sql-agent ask "Покажи тренды продаж по месяцам"
sql-agent ask "В каком регионе самый высокий средний чек?"
```

3. **Исследуйте структуру данных:**
```bash
sql-agent schema sales
sql-agent schema  # Показать все таблицы
```

## Команды

### `load` - Импорт CSV данных

Загрузить CSV файл в базу данных SQLite:

```bash
sql-agent load <csv_файл> [ОПЦИИ]

Опции:
  --table, -t      Название таблицы (по умолчанию: имя файла)
  --db            Путь к файлу базы данных (по умолчанию: app.db)
  --verbose, -v   Включить подробное логирование
```

Примеры:
```bash
sql-agent load sales_data.csv --table sales
sql-agent load /path/to/products.csv --table products --db mydata.db
```

### `ask` - Запросы к данным

Задавайте вопросы на естественном языке о ваших данных:

```bash
sql-agent ask "<вопрос>" [ОПЦИИ]

Опции:
  --table, -t     Конкретная таблица для запроса
  --db           Путь к файлу базы данных (по умолчанию: app.db)
  --api-key      OpenAI API ключ (переопределяет переменную окружения)
  --model, -m    Модель OpenAI (по умолчанию: gpt-4)
  --verbose, -v  Включить подробное логирование
  --debug        Включить режим отладки
```

Примеры:
```bash
sql-agent ask "Какая общая выручка по категориям?"
sql-agent ask "Покажи товары с ценой больше $100" --table products
sql-agent ask "Тренд продаж по месяцам во втором квартале" --debug
```

### `schema` - Исследование структуры базы данных

Просмотр структуры таблиц и примеров данных:

```bash
sql-agent schema [название_таблицы] [ОПЦИИ]

Опции:
  --db           Путь к файлу базы данных (по умолчанию: app.db)
  --verbose, -v  Включить подробное логирование
```

Примеры:
```bash
sql-agent schema              # Список всех таблиц
sql-agent schema sales        # Подробная информация о таблице 'sales'
```

## Демо данные

Проект включает образец `sales.csv` с 50 записями данных электронной коммерции:

- Информация о товарах (название, категория, цена)
- Данные продаж (количество, дата, клиент)
- География (регионы) и данные команды продаж

Попробуйте эти примеры вопросов:
- "Какие топ-5 категорий по выручке?"
- "Покажи результаты продаж по регионам"
- "У какого менеджера по продажам самые высокие продажи?"
- "Какой средний чек по месяцам?"

## Пример сессии

```bash
# Загружаем демо данные
$ sql-agent load data/sales.csv --table sales
✅ Successfully loaded 'sales'
Table Name: sales
Rows: 50
Columns: 9
Time: 0.12s

# Спрашиваем про топ категории
$ sql-agent ask "Какие топ-5 категорий по общей выручке?"

💡 Ответ
На основе данных продаж, вот топ-5 категорий по общей выручке:

1. Electronics: $15,447.42 (60.8% от общей суммы)
2. Footwear: $4,087.34 (16.1% от общей суммы)  
3. Clothing: $3,982.38 (15.7% от общей суммы)

Категория Electronics доминирует с более чем 60% общей выручки, 
в основном за счет дорогих товаров как ноутбуки, телефоны и игровые консоли.

# Исследуем структуру данных
$ sql-agent schema sales
📊 Table: sales
Columns: id, product_name, category, price, quantity, sale_date, customer_id, region, salesperson
Примеры данных отображены...
```

## Особенности безопасности

- **Защита от SQL инъекций**: Все запросы валидируются и санитизируются
- **Подход белого списка**: Разрешены только SELECT запросы
- **Ограничения строк**: Максимум 100 строк на запрос (применяется автоматически)
- **Таймауты запросов**: 10-секундный лимит выполнения
- **Обнаружение паттернов**: Блокирует опасные SQL паттерны

## Архитектура

```
sql_agent/
├── app/
│   ├── cli.py          # Интерфейс CLI на Typer
│   ├── db.py           # Менеджер базы данных SQLite
│   ├── llm.py          # Интеграция LangChain + OpenAI
│   ├── tools.py        # Инструменты LangChain для SQL операций
│   ├── validators.py   # Валидация безопасности SQL
│   └── models.py       # Модели данных Pydantic
├── data/
│   └── sales.csv       # Демо датасет
└── pyproject.toml      # Зависимости и конфигурация
```

## Технологический стек

- **Python 3.12+** - Современный Python с типизацией
- **LangChain** - Оркестрация LLM и tool calling
- **OpenAI GPT-4** - Понимание естественного языка
- **SQLite** - Встроенная база данных (не требует настройки)
- **Typer** - Современный CLI фреймворк с автодополнением
- **Rich** - Красивое форматирование терминала
- **Pandas** - Эффективная обработка CSV
- **Pydantic** - Валидация и сериализация данных

## Конфигурация

Переменные окружения (опциональный файл `.env`):

```bash
# Обязательно
OPENAI_API_KEY=sk-...

# Опционально
DEFAULT_DB_PATH=app.db
LLM_MODEL=gpt-4
LLM_TEMPERATURE=0.1
MAX_QUERY_ROWS=100
DEBUG=false
LOG_LEVEL=INFO
```

## Решение проблем

### Частые проблемы

**"OpenAI API key is required"**
- Установите переменную окружения `OPENAI_API_KEY` или используйте опцию `--api-key`

**"Database not found"**
- Сначала запустите `sql-agent load` для создания базы данных
- Проверьте путь к базе данных с помощью опции `--db`

**"Table not found"**
- Используйте `sql-agent schema` для просмотра доступных таблиц
- Проверьте правильность написания названия таблицы

**"Invalid SQL query"**
- ИИ сгенерировал некорректный SQL - попробуйте переформулировать вопрос
- Используйте `--debug` чтобы увидеть сгенерированный SQL

### Режим отладки

Включите подробное логирование для диагностики проблем:

```bash
sql-agent ask "ваш вопрос" --debug
```

Это покажет:
- Сгенерированные SQL запросы
- Время выполнения
- Последовательность вызова инструментов
- Детали ошибок

## О проекте

Это пет-проект, демонстрирующий анализ данных с помощью ИИ. Кодовая база разработана для обучения и демонстрации современных практик разработки на Python.

Ключевые концепции:
- LangChain tool calling
- Безопасная валидация SQL запросов
- Дизайн CLI приложений
- Типобезопасная разработка на Python

## Лицензия

MIT License - см. файл LICENSE для деталей.

---

**Работает на OpenAI GPT-4 🤖**